### Configuration

`project_id`: Name of the Google Cloud project id in which the BigQuery should be loaded
`schema`: Name of the dataset to write the streams to
`loading_deck_gcs_bucket_name`: Name of the GCS bucket in which the target will write the data before merging it in BigQuery